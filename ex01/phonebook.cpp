#include "phonebook.hpp"

int main()
{
    std::string cmd;
    std::cout << "Scegli tra add; search; exit" << std::endl;
    std::cin >> cmd;
    if (cmd == "add")
    {
        std::cout << cmd << std::endl;
    }
    // else if ()
    // else if ()
    // else 
    //     vaffanculo
    return 0;
}